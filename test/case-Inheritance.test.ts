
describe('case inheritance', () => {
	beforeEach(() => {

	})

	it('add some test for module providers order and cloning providers from modules', () => {

	})

	it('pure modules should convert singletons to scoped', () => {

	})
})
